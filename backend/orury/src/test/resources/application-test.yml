## 테스트용 데이터베이스 설정
#spring:
#  datasource:
#    driver-class-name: org.h2.Driver
#    url: jdbc:h2:mem:testdb
#    username: sa
#    password:
#  h2:
#    console:
#      enabled: true # H2 콘솔 활성화 (테스트 중에 데이터베이스 확인용)
#
## JPA 설정
#jpa:
#  hibernate:
#    ddl-auto: update # 테스트 중에 스키마 자동 업데이트 (create 또는 update 사용)
#
## 로깅 설정
#logging:
#  level:
#    org.springframework: INFO
#    com.example: DEBUG # 필요한 패키지 레벨 로깅 설정
#
## 기타 설정 (필요에 따라 추가)
spring:
  jpa:
    show-sql: true
    properties:
      format_sql: true
      dialect: org.hibernate.dialect.MySQL8Dialect
      hibernate:
        ddl-auto: validate
        use_sql_comments: true
        show_sql: true
        format_sql: true
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: ${LOCAL_DB_URL} # configuration에서 설정 필요
    username: ${LOCAL_DB_USERNAME}
    password: ${LOCAL_DB_USER_PASSWORD}